{
  "name": "sf-manager",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.0"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10.8.2"
}
