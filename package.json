{
  "name": "sf-manager",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^6.0.3",
    "eslint": "^10.8.1",
    "prettier": "^3.4.0"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "npm@10.8.2"
}
