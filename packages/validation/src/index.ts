// Stub package. Will hold shared Zod schemas (ADR-015), the single source of
// truth for validation on both the NestJS backend and every frontend client,
// once there's a real domain field to validate.
export {};
